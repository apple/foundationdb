[configuration]
config = 'single memory-radixtree perpetual_storage_wiggle_engine=memory-radixtree perpetual_storage_wiggle=0'
singleRegion = true

[[test]]
testTitle = 'StatusPerpetualWiggleEngine'
useDB = true

    [[test.workload]]
    testName = 'Status'
    testDuration = 10.0
    requestsPerSecond = 2.0
    enableLatencyBands = false
