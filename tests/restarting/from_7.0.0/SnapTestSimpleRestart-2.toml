[configuration]
buggify=false

# Verify all keys are even numbered
[[test]]
testTitle='SnapSimpleVerify'

    [[test.workload]]
    testName='SnapTest'
    numSnaps=1
    maxSnapDelay=3.0
    testID=3
    enableDD=true
