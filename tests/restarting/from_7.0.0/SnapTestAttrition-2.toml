[configuration]
buggify = false

# verify all keys are even numbered
[[test]]
testTitle = 'SnapTestVerify'

    [[test.workload]]
    testName = 'SnapTest'
    numSnaps = 1
    maxSnapDelay = 3.0
    testID = 3
    restartInfoLocation = 'simfdb/restartInfo.ini'
    enableDD = true
