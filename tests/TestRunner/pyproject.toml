[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "fdb-test-runner"
version = "0.1.0"
description = "FoundationDB Test Runner utilities"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["fdb_test_runner*"]
