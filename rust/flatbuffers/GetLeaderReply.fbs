include "common.fbs";

// See field documentation in CoordinationInterface.h
table LeaderInfo {
    change_id: UID;
    serialized_info: [ubyte];
    forward: bool;
}

union Optional {
    LeaderInfo, Void
}

table EnsureTable {
    optional: Optional;
}

union ErrorOr { Error, EnsureTable }

table FakeRoot {
    error_or: ErrorOr;
}
root_type FakeRoot;